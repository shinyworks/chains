% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/is_default.R
\name{is_default}
\alias{is_default}
\title{Determine whether a reactive is using its default value}
\usage{
is_default(x, ...)
}
\arguments{
\item{x}{(\code{any}) The object to test.}

\item{...}{Additional arguments passed to methods.}
}
\value{
A \code{logical} value indicating whether the object is currently using its
default value.
}
\description{
A convenience function to check whether an object is currently using its
default value. A method is implemented for \code{\link[=validated_reactive_val]{validated_reactive_val()}} objects.
}
\examples{
vrv <- validated_reactive_val(
  value = "good",
  default = "default",
  validation_expr = {
    if (.vrv() == "bad") {
      rlang::abort("is bad")
    }
    .vrv()
  }
)
shiny::isolate(vrv())
shiny::isolate(is_default(vrv))
vrv("bad")
shiny::isolate(vrv())
shiny::isolate(is_default(vrv))
}
