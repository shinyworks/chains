% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_error.R
\name{extract_error}
\alias{extract_error}
\title{Extract an error message from an object}
\usage{
extract_error(x, ..., capture = TRUE)
}
\arguments{
\item{x}{(\code{any}) The object from which to extract the error message.}

\item{...}{Additional arguments passed to methods.}

\item{capture}{(length-1 \code{logical}) If \code{TRUE}, the error is captured and
returned with class \code{captured-error} (as well as \verb{captured-} prepended on
any error subclasses). If \code{FALSE}, the error is thrown.}
}
\value{
If the object contains an error message, an object with class
\code{captured-error} if \code{capture} is \code{TRUE} or the error condition if \code{capture}
is \code{FALSE}. Depending on your purpose, you may need to \code{\link[=signalCondition]{signalCondition()}}
or \code{\link[rlang:cnd_signal]{rlang::cnd_signal()}} to actually signal the error. If the object does
not contain an error, \code{NULL}.
}
\description{
A convenience function to extract the error message attached to an object. A
method is implemented for \code{\link[=validated_reactive_val]{validated_reactive_val()}} objects.
}
\examples{
vrv <- validated_reactive_val(
  value = "good",
  default = "default",
  validation_expr = {
    if (.vrv() == "bad") {
      rlang::abort("is bad", class = "special_error")
    }
    .vrv()
  }
)
shiny::isolate(extract_error(vrv))
vrv("bad")
shiny::isolate(vrv())
captured_error <- shiny::isolate(extract_error(vrv))
class(captured_error)
captured_error$message
raw_error <- shiny::isolate(extract_error(vrv, capture = FALSE))
try(rlang::cnd_signal(raw_error))
}
